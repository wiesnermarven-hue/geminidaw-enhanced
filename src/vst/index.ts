// VST placeholder module
export const loadVstPlugin = async (url: string) => {
  console.warn('VST loading not implemented, URL:', url);
  // Future implementation could use WebAudio Modules (WAM) or WebAssembly
};
